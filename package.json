{
  "name": "new-ignition-fronted",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "npx pug src --pretty --out dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
